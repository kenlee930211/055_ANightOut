export enum Status {
    Active = 1,
    Inactive = 0,
}

export enum BetType {
    DoubleHalf = 1,
    Double = 2,
}

export enum CardType {
    Spade = 4,
    Heart = 3,
    Club = 2,
    Diamond = 1,
}
